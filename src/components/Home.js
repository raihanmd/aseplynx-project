import { Link } from "react-router-dom";
import Navbar from "../utils/Navbar";

const Home = () => {
  return (
    <div className="home">
      <Navbar />
      <h2>This is a Home</h2>
    </div>
  );
};

export default Home;
